{
  "name": "jasca",
  "version": "0.0.1",
  "private": true,
  "description": "Trivy Vulnerability Management System",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean && rimraf node_modules",
    "bundle:offline": "powershell -ExecutionPolicy Bypass -File script/build-offline-bundle.ps1"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "turbo": "^2.3.3",
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@9.15.1",
  "engines": {
    "node": ">=20.0.0"
  },
  "pnpm": {
    "overrides": {
      "qs": ">=6.14.1"
    }
  }
}
