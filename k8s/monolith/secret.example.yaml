apiVersion: v1
kind: Secret
metadata:
  name: jasca-secret
  namespace: jasca
type: Opaque
stringData:
  # Keep these values stable across redeployments.
  JWT_SECRET: "replace-with-long-random-secret"
  DB_PASSWORD: "replace-with-current-or-new-db-password"
  ZAP_API_KEY: ""
